{
  "rules": {
    "games": {
      "$gameId": {
        ".read": true,
        ".write": "auth != null",
        "operatorUid": {
          ".validate": "!data.exists() || data.val() === auth.uid"
        }
      }
    }
  }
}
