{
  "rules": {
    "games": {
      "$gameId": {
        ".read": true,
        ".write": "auth != null && (!data.child('operatorUid').exists() || data.child('operatorUid').val() === auth.uid)"
      }
    }
  }
}
